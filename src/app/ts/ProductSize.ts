export interface ProductSize {
    productSizeID: number;        // ID sản phẩm
    quantity: number;             // Số lượng sản phẩm
    price: number;                // Giá sản phẩm
    variant: string;
    discountPrice: number;        // Giá sản phẩm giảm
}